import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // Allow local static imports (handled by Next.js automatically)
    // Add external domains here if needed in the future
  },
};

export default nextConfig;
